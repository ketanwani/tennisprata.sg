services:
  web:
    command: gunicorn tennisprata.wsgi:application --bind 0.0.0.0:8000 --workers 2 --timeout 60
    env_file:
      - .env
    ports:
      - "127.0.0.1:8000:8000"

  celery:
    env_file:
      - .env

  celery-beat:
    env_file:
      - .env
